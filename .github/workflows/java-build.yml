name: Java Build with Maven

on:
  push:
    branches:
      - '*'
  pull_request:
    branches:
      - '*'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven

      - name: Set up Apache Spark
        uses: vemonet/setup-spark@v1
        with:
          spark-version: '3.5.1'
          hadoop-version: '3'
          scala-version: '2.13'
          log-level: 'debug'
          install-folder: '/home/runner/work'

      - name: Build with Maven
        run: mvn -B clean package
        env:
          SPRING_DATASOURCE_URL: jdbc:oracle:thin:@//localhost:1521/oracle
          SPRING_DATASOURCE_USERNAME: ${{ secrets.DB_USERNAME_OR }}
          SPRING_DATASOURCE_PASSWORD: ${{ secrets.DB_PASSWORD_OR }}
          JIRA_EMAIL: ${{ secrets.JIRA_EMAIL }}
          JIRA_TOKEN: ${{ secrets.JIRA_TOKEN }}
          MAVEN_OPTS: "--add-exports=java.base/sun.nio.ch=ALL-UNNAMED"

      - name: Verify Spark Installation
        run: spark-submit --version
        
      - name: Build Docker image (Server)
        run: |
          docker build -t dashflow-server:latest .
          docker save dashflow-server:latest | gzip > dashflow-server.tar.gz
          
      - name: Transfer Docker image via SCP
        uses: appleboy/scp-action@v0.1.4
        with:
          host: ${{ secrets.MANOLITO_VM_IP }}
          username: ${{ secrets.MANOLITO_VM_USR }}
          password: ${{ secrets.MANOLITO_VM_PSW }}
          source: "dashflow-server.tar.gz"
          target: "/opt/dashflow-server/"
